# 360도 폭발형 펀치 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 차징 후 릴리즈 시 정면(PUNCH_ARC) 대신 360도 전방위로 주변 적을 넉백시키는 폭발형 공격으로 변경한다.

**Architecture:** 서버의 두 곳(플레이어 punch-release 핸들러, 봇 펀치 로직)에서 PUNCH_ARC 방향 체크를 제거하고, 넉백 방향을 펀처의 facing이 아닌 타겟-펀처 간 방사형 벡터로 변경한다. 클라이언트에서는 펀치 발동 시 360도 충격파 이펙트를 추가한다.

**Tech Stack:** Node.js (server.js), Three.js (game.js), Socket.io

---

## 파일 구조

| 파일 | 변경 유형 | 책임 |
|------|-----------|------|
| `server.js:36` | Modify | PUNCH_ARC 상수 — 제거 또는 주석 처리 |
| `server.js:565-577` | Modify | 봇 펀치 히트 판정 — 방향 체크 제거, 방사형 넉백 |
| `server.js:832-858` | Modify | 플레이어 펀치 히트 판정 — 방향 체크 제거, 방사형 넉백 |
| `public/js/game.js:1383-1387` | Modify | 펀치 릴리즈 시 360도 충격파 이펙트 |

---

### Task 1: 서버 — 플레이어 펀치를 360도 방사형으로 변경

**Files:**
- Modify: `server.js:36` (PUNCH_ARC 상수)
- Modify: `server.js:832-858` (punch-release 핸들러 히트 판정)

- [ ] **Step 1: PUNCH_ARC 상수를 주석 처리**

`server.js:36`에서:
```js
// 변경 전:
const PUNCH_ARC = 0.45;        // cos(angle) threshold - must face target

// 변경 후:
// const PUNCH_ARC = 0.45;     // [360도 폭발형으로 변경] 더 이상 사용하지 않음
```

- [ ] **Step 2: punch-release 핸들러에서 방향 체크 제거 + 방사형 넉백**

`server.js:832-858`에서:
```js
// 변경 전 (L832-858):
    // Check hit on all nearby alive players
    let hit = false;
    for (const target of room.players.values()) {
      if (target.id === player.id || !target.alive) continue;
      if (target.isInvincible) continue;

      const dx = target.x - player.x;
      const dy = target.y - player.y;
      const dist = Math.sqrt(dx * dx + dy * dy);

      if (dist > PUNCH_RANGE) continue;

      // Direction check
      if (dist > 0.01) {
        const dot = (dx / dist) * nfx + (dy / dist) * nfy;
        if (dot < PUNCH_ARC) continue;
      }

      // HIT! Knockback toward punch direction
      target.vx += nfx * punchForce;
      target.vy += nfy * punchForce;
      hit = true;
    }

    // Recoil on puncher
    player.vx -= nfx * (hit ? 3 : 1);
    player.vy -= nfy * (hit ? 3 : 1);

// 변경 후:
    // 360도 폭발형 — 범위 내 모든 적에게 방사형 넉백
    let hitCount = 0;
    for (const target of room.players.values()) {
      if (target.id === player.id || !target.alive) continue;
      if (target.isInvincible) continue;

      const dx = target.x - player.x;
      const dy = target.y - player.y;
      const dist = Math.sqrt(dx * dx + dy * dy);

      if (dist > PUNCH_RANGE) continue;

      // 방사형 넉백: 펀처→타겟 방향으로 밀어냄
      if (dist > 0.01) {
        target.vx += (dx / dist) * punchForce;
        target.vy += (dy / dist) * punchForce;
      } else {
        // 완전 겹친 경우 랜덤 방향
        const rAngle = Math.random() * Math.PI * 2;
        target.vx += Math.cos(rAngle) * punchForce;
        target.vy += Math.sin(rAngle) * punchForce;
      }
      hitCount++;
    }

    // 반동: 히트 수에 비례 (다수 히트 시 제자리 고정)
    if (hitCount > 0) {
      player.vx -= nfx * Math.min(hitCount * 2, 6);
      player.vy -= nfy * Math.min(hitCount * 2, 6);
    }
```

- [ ] **Step 3: facing 방향 계산 코드 정리**

`server.js:825-830`의 facing 방향 계산(`nfx`, `nfy`)은 여전히 반동(recoil)에 사용되므로 유지한다. 변경 없음. 확인만 하면 됨.

- [ ] **Step 4: 서버 시작하여 문법 에러 없는지 확인**

Run: `cd /Users/user/Documents/workspace/noon-game && node -c server.js`
Expected: 출력 없음 (문법 에러 없음)

- [ ] **Step 5: Commit**

```bash
git add server.js
git commit -m "feat: 플레이어 펀치를 360도 방사형 폭발로 변경"
```

---

### Task 2: 서버 — 봇 펀치도 360도 방사형으로 변경

**Files:**
- Modify: `server.js:565-577` (봇 펀치 히트 판정)

- [ ] **Step 1: 봇 펀치의 방향 체크 제거 + 방사형 넉백**

`server.js:565-577`에서:
```js
// 변경 전 (L565-577):
        for (const t2 of room.players.values()) {
          if (t2.id === bot.id || !t2.alive || t2.isInvincible) continue;
          const hx = t2.x - bot.x;
          const hy = t2.y - bot.y;
          const hd = Math.sqrt(hx * hx + hy * hy);
          if (hd > PUNCH_RANGE) continue;
          if (hd > 0.01) {
            const dot = (hx / hd) * nfx + (hy / hd) * nfy;
            if (dot < PUNCH_ARC) continue;
          }
          t2.vx += nfx * force;
          t2.vy += nfy * force;
        }

// 변경 후:
        for (const t2 of room.players.values()) {
          if (t2.id === bot.id || !t2.alive || t2.isInvincible) continue;
          const hx = t2.x - bot.x;
          const hy = t2.y - bot.y;
          const hd = Math.sqrt(hx * hx + hy * hy);
          if (hd > PUNCH_RANGE) continue;
          // 360도 방사형 넉백
          if (hd > 0.01) {
            t2.vx += (hx / hd) * force;
            t2.vy += (hy / hd) * force;
          } else {
            const rAngle = Math.random() * Math.PI * 2;
            t2.vx += Math.cos(rAngle) * force;
            t2.vy += Math.sin(rAngle) * force;
          }
        }
```

- [ ] **Step 2: 문법 확인**

Run: `cd /Users/user/Documents/workspace/noon-game && node -c server.js`
Expected: 출력 없음

- [ ] **Step 3: Commit**

```bash
git add server.js
git commit -m "feat: 봇 펀치도 360도 방사형으로 통일"
```

---

### Task 3: 클라이언트 — 360도 충격파 이펙트

**Files:**
- Modify: `public/js/game.js:1383-1387` (펀치 릴리즈 이펙트)
- Modify: `public/js/game.js:1053-1072` 근처 (충격파 링 함수 추가)

- [ ] **Step 1: 360도 충격파 링 스폰 함수 추가**

`public/js/game.js`의 `spawn3DParticles` 함수 바로 아래(L1072 부근)에 추가:
```js
  // 360도 충격파 링 이펙트
  function spawnShockwaveRing(sx, sy, count, color, radius, speedMult) {
    const { x: wx, z: wz } = serverToWorld(sx, sy);
    const col = new THREE.Color(color || '#ff4400');
    for (let i = 0; i < count; i++) {
      const angle = (i / count) * Math.PI * 2;
      const geo = new THREE.SphereGeometry(0.6 + Math.random() * 0.4, 4, 4);
      const mat = new THREE.MeshBasicMaterial({ color: col, transparent: true, opacity: 1 });
      const mesh = new THREE.Mesh(geo, mat);
      const sp = (1.5 + Math.random() * 0.5) * (speedMult || 1);
      mesh.position.set(
        wx + Math.cos(angle) * radius * 0.3,
        5 + Math.random() * 3,
        wz + Math.sin(angle) * radius * 0.3
      );
      scene.add(mesh);
      particles3d.push({
        mesh,
        vx: Math.cos(angle) * sp,
        vy: Math.random() * 0.5 + 0.2,
        vz: Math.sin(angle) * sp,
        life: 1, decay: 0.02 + Math.random() * 0.01,
      });
    }
  }
```

- [ ] **Step 2: 펀치 릴리즈 이펙트를 충격파 링으로 교체**

`public/js/game.js`의 펀치 릴리즈 이벤트 핸들러(L1383-1387 부근)에서:
```js
// 변경 전:
        spawn3DParticles(me.x, me.y, count, charge > 0.6 ? '#ff2200' : '#ff8844', 0.8 + charge * 1.5);

// 변경 후:
        // 360도 충격파 링 + 중심부 폭발 파티클
        const blastColor = charge > 0.6 ? '#ff2200' : '#ff8844';
        spawnShockwaveRing(me.x, me.y, 16 + Math.floor(charge * 16), blastColor, 8, 0.8 + charge * 1.5);
        spawn3DParticles(me.x, me.y, Math.floor(count * 0.5), blastColor, 0.5);
```

- [ ] **Step 3: 문법 확인**

Run: `cd /Users/user/Documents/workspace/noon-game && node -c public/js/game.js`
Expected: 출력 없음

- [ ] **Step 4: 브라우저에서 시각 확인**

Run: `cd /Users/user/Documents/workspace/noon-game && node server.js`
- localhost:3000 접속
- 방 생성 후 차징 → 릴리즈 시 360도 원형으로 파티클이 퍼져나가는지 확인
- 서버 종료

- [ ] **Step 5: Commit**

```bash
git add public/js/game.js
git commit -m "feat: 펀치 릴리즈 시 360도 충격파 링 이펙트 추가"
```

---

### Task 4: PUNCH_ARC 참조 정리

**Files:**
- Modify: `server.js` (미사용 PUNCH_ARC 참조 확인)

- [ ] **Step 1: PUNCH_ARC가 더 이상 참조되지 않는지 확인**

Run: `grep -n "PUNCH_ARC" server.js`
Expected: 주석 처리된 상수 선언 1줄만 남아야 함. 다른 참조가 있으면 제거.

- [ ] **Step 2: Commit (필요시)**

```bash
git add server.js
git commit -m "chore: 미사용 PUNCH_ARC 참조 정리"
```
